apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # Replace these key-value pairs with your actual configuration
  DATABASE_URL: "jdbc:mysql://localhost:3306/mydatabase"
  APP_ENV: "production"
  FEATURE_FLAG: "true"

